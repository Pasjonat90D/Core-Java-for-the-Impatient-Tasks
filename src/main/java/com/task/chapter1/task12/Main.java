package com.task.chapter1.task12;

public class Main {
//      code from src.zip without continue and break
//
//      for (;wc <= maximumPoolSize && ! (timedOut && timed);) {
//        int wc = workerCountOf(c);
//        timed = allowCoreThreadTimeOut || wc > corePoolSize;
//        if (compareAndDecrementWorkerCount(c))
//            return null;
//        c = ctl.get();  // Re-read ctl
//
//    }

}
